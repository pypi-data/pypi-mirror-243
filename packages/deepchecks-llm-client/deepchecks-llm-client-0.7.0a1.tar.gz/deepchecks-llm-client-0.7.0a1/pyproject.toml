[build-system]
requires = [
    "setuptools<69.0.0",
    "dunamai",
    "wheel",
]

[project]
name = 'deepchecks-llm-client'
dynamic = ['version']

[project.optional-dependencies]
langchin = [
    "langchain>=0.0.313,<0.1",
]