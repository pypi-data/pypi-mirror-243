[project]
name = "pride-checksum"
version = "1.0.0"
description = "Computes SHA-1 checksum for all the specified files  and writes to checksum.txt file."
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "click"
]

[project.scripts]
pride_checksum = "pride_checksum.__main__:main"