from setuptools import setup, find_packages

setup(
    name='flowa',
    version='0.1.0a',
    description='flowa pre-setup',
    url='https://github.com/',
    author='flowa',
    author_email='flowa@flowa.com',
    license='MIT',
    packages=find_packages(),
    zip_safe=False,
    install_requires=[],
    classifiers=[],
)