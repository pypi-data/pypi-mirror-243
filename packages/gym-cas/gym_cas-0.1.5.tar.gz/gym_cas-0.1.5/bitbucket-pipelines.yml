image: python:3.11

pipelines:
    default:
        - step:
            name: Test
            caches:
                - pip
            script:
                - pip install hatch
                - hatch build
                - hatch run test
