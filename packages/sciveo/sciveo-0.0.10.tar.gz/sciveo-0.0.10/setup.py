from setuptools import setup, find_packages

setup(
    name='sciveo',
    version='0.0.10',
    packages=find_packages(),
    install_requires=[
      'pandas>=0.0.0',
      'numpy>=0.0.0',
      'requests>=0.0.0',
    ],
)
