[tool.poetry]
name = "publishing-test"
version = "0.1.0"
description = ""
authors = ["Paul Armstrong <parmstrong@gitlab.com>"]
readme = "README.md"
packages = [{include = "publishing_test"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
